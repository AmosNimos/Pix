The canvas is the drawable area where pixels can be placed,
The palette is the colors available using the number key from 0 to 9 it can be loaded from a file as a list of hex color values.
The palette can be edited by manually entering hex value and assigning them to a number key (+ shift to assign current color)

Tool selection:
RegularPen  " "
RectPen     "R"


# To implement
EllipsePen  "E"
LinePen     "L"
ColorPicker "C"

